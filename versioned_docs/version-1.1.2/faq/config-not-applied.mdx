---
title: "配置无法生效"
sidebar_position: 2
---
正常配置触发键和宏配置后，按键无法触发宏配置

**解决方法：**

- 右侧`禁用`取消勾选
- 模块中`禁用`取消勾选
- 模块中的`前台`内容清空（除非你知道前台的作用）
- 配置后需要`应用并保存`配置才会生效

> 提示：不支持覆盖文件更新软件，覆盖内容更新也出现宏触发无效（请使用软件的配置迁移，或导出导入同步配置）
